// Regenerate via:
//   supabase gen types typescript --project-id <ref> --schema public > lib/supabase/types.ts
// Until then this is an empty placeholder so server.ts and client.ts can
// be typed against `Database` without errors.
export type Database = Record<string, never>;
